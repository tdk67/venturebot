{
  "name": "venturebot-frontend",
  "version": "0.1.0",
  "private": true,
  "description": "VentureBot rewrite frontend — plain TypeScript, no framework (decision D6). Bundled with esbuild to static/app.js (keeps CSP script-src 'self').",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && esbuild src/app.ts --bundle --minify --target=es2020 --outfile=../static/app.js"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "^7.0.2"
  }
}

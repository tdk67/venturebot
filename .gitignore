# ── Secrets — never commit ──────────────────────────────
.env
.env.*
!.env.example

# ── Runtime state & generated artifacts ──────────────────
state.json
*.db
*.sqlite
data/
workspace/
__pycache__/
.pytest_cache/
*.pyc

# ── Python env ──────────────────────────────────────────
venv/
.venv/
backups/

# local-only disabled tests
_auth_tests_disabled/

# ── Firebase Hosting ────────────────────────────────────
# The `public/` placeholder is never served (all traffic is rewritten to
# Cloud Run), but Firebase requires the directory to exist at deploy time.
public/
logs/
